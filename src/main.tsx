import React from 'react'
import ReactDOM from 'react-dom/client'
import GuitarScaleApp from '../remixed-7682492a.tsx'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GuitarScaleApp />
  </React.StrictMode>,
)
